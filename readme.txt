This program calculates the signature files in a directory and subdirectories in which it is located.
The program checks the signature every 10 seconds, if the signatures do not match, it displays a list of files and the number of iteration.
if you want to close it, just close up;